# Prefer apps/web/.env.example for the Next.js app (Oracle, Upstash, Sentry).
# This root file is only for the optional Rust API + shared notes.

# Web — see apps/web/.env.example
# NEXT_PUBLIC_SENTRY_DSN=

# API (Rust Axum Backend) — optional
DATABASE_URL=postgresql://mythos:mythos@localhost:5435/mythos_atlas
RUST_LOG=info
JWT_SECRET=dev_change_me_in_production
CORS_ORIGIN=http://localhost:3000
